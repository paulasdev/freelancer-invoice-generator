@page
@model LoginModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Login | SoloBill</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap & Google Fonts -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter&family=Poppins:wght@500;700&display=swap" rel="stylesheet" />

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f2f3f5;
        }

        .login-container {
            min-height: 100vh;
        }

        .left-panel {
            background-color: #ffffff;
            padding: 3rem;
        }

        .right-panel {
            background-color: #014A59;
            color: white;
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-control:focus {
            box-shadow: none;
        }

        .brand {
            font-weight: bold;
            font-size: 1.5rem;
        }

        a {
            text-decoration: none;
        }

        .btn-outline-secondary {
            width: 100%;
        }
    </style>
</head>
<body>

<div class="container-fluid login-container d-flex">
    <div class="col-md-6 left-panel">
        <div class="brand mb-4">SoloBill</div>
        <h3 class="mb-4">Welcome Back!</h3>
        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="form-group form-check mb-3">
                <input asp-for="Input.RememberMe" class="form-check-input" />
                <label asp-for="Input.RememberMe" class="form-check-label"></label>
                <a class="float-end" href="/Identity/Account/ForgotPassword">Forgot password?</a>
            </div>

            <button type="submit" class="btn btn-dark w-100">Log In</button>
        </form>

        <div class="text-center my-4 position-relative">
  <hr class="my-0" />
  <span style="position: absolute; top: -0.75rem; left: 50%; transform: translateX(-50%); background: #fff; padding: 0 1rem; font-size: 0.9rem; color: #6c757d;">
    or
  </span>
</div>

        <div class="d-flex justify-content-center gap-2">
            <button class="btn btn-outline-secondary">Google</button>
            <button class="btn btn-outline-secondary">Facebook</button>
        </div>

        <div class="text-center mt-4">
            Don’t have an account? <a href="/Identity/Account/Register">Sign Up</a>
        </div>
    </div>

    <div class="col-md-6 right-panel text-center">
        <h4>Inventory Management</h4>
        <p class="mt-2">Join SoloBill to streamline your freelance billing process and enhance your productivity.</p>
    </div>
</div>

</body>
</html>