@using Microsoft.AspNetCore.Identity
@using SoloBill.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - SoloBill</title>

    <!-- Bootstrap + Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />

    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link href="https://fonts.googleapis.com/css2?family=Inter&display=swap" rel="stylesheet" />

    @RenderSection("Head", required: false)
</head>
<body>
    <!-- Top Navbar -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center" href="/">
                    <img src="~/images/solobill-logo.png" alt="SoloBill Logo" style="height: 40px;" class="me-2" />
                </a>

                <!-- Mobile toggles -->
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <!-- Button to open offcanvas (mobile sidebar) -->
                    <button class="btn d-lg-none me-2" type="button"
                            data-bs-toggle="offcanvas" data-bs-target="#sbOffcanvas"
                            aria-controls="sbOffcanvas" aria-label="Open menu">
                        <i class="bi bi-list fs-3"></i>
                    </button>
                }

                <!-- Standard navbar collapse (holds auth buttons) -->
                <button class="navbar-toggler" type="button"
                        data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                    <div class="d-flex align-items-center gap-3 py-3 py-lg-0">
                        @if (SignInManager.IsSignedIn(User))
                        {
                            <form method="post"
                                  asp-area="Identity"
                                  asp-page="/Account/Logout"
                                  asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })"
                                  class="d-inline">
                                <button type="submit" class="btn btn-outline-secondary rounded-pill px-4 py-2">
                                    Log out
                                </button>
                            </form>
                        }
                        else
                        {
                            <a href="/Identity/Account/Login" class="btn btn-outline-primary rounded-pill px-4 py-2">
                                Sign In
                            </a>
                            <a href="/Identity/Account/Register" class="btn btn-primary rounded-pill px-4 py-2">
                                Register
                            </a>
                        }
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Mobile offcanvas (shows the sidebar links on small screens) -->
    @if (User.Identity?.IsAuthenticated == true)
    {
        <div class="offcanvas offcanvas-start" tabindex="-1" id="sbOffcanvas" aria-labelledby="sbOffcanvasLabel">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title" id="sbOffcanvasLabel">Menu</h5>
                <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body d-flex flex-column">
                <div class="mb-3">
                    <a href="/Invoices/Create" class="btn btn-purple w-100 rounded-pill">
                        <i class="bi bi-plus-lg me-1"></i> New invoice
                    </a>
                </div>

                <div class="text-uppercase small fw-semibold text-muted px-1 mb-1">Overview</div>
                <a class="nav-link py-2 @(ViewContext.RouteData.Values["controller"]?.ToString()=="Dashboard" ? "active fw-semibold" : "")" href="/Dashboard">
                    <i class="bi bi-grid me-2"></i> Dashboard
                </a>

                <div class="text-uppercase small fw-semibold text-muted px-1 mt-3 mb-1">Work</div>
                <a class="nav-link py-2 @(ViewContext.RouteData.Values["controller"]?.ToString()=="Invoices" ? "active fw-semibold" : "")" href="/Invoices">
                    <i class="bi bi-file-earmark-text me-2"></i> Invoices
                </a>
                <a class="nav-link py-2 @(ViewContext.RouteData.Values["controller"]?.ToString()=="Clients" ? "active fw-semibold" : "")" href="/Clients">
                    <i class="bi bi-people me-2"></i> Clients
                </a>

                <div class="text-uppercase small fw-semibold text-muted px-1 mt-3 mb-1">Account</div>
                <a class="nav-link py-2 @(ViewContext.RouteData.Values["controller"]?.ToString()=="ApplicationUser" ? "active fw-semibold" : "")" href="/ApplicationUser/Settings">
                    <i class="bi bi-gear me-2"></i> Settings
                </a>

                <div class="mt-auto pt-3 small text-muted">
                    <i class="bi bi-shield-lock me-1"></i> Secure by design
                </div>
            </div>
        </div>
    }

    <!-- Flash messages -->
    <div class="container mt-3" id="flash-area">
        @* SUCCESS *@
        @if (TempData["ToastSuccess"] is string s1 && !string.IsNullOrWhiteSpace(s1))
        {
            <div class="alert alert-sb alert-sb-primary alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-check2-circle"></i> @s1
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["SuccessMessage"] is string s2 && !string.IsNullOrWhiteSpace(s2))
        {
            <div class="alert alert-sb alert-sb-primary alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-check2-circle"></i> @s2
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        @* WARNING *@
        @if (TempData["ToastWarning"] is string w1 && !string.IsNullOrWhiteSpace(w1))
        {
            <div class="alert alert-sb alert-sb-warning alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-exclamation-triangle"></i> @w1
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["WarningMessage"] is string w2 && !string.IsNullOrWhiteSpace(w2))
        {
            <div class="alert alert-sb alert-sb-warning alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-exclamation-triangle"></i> @w2
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        @* INFO *@
        @if (TempData["ToastInfo"] is string i1 && !string.IsNullOrWhiteSpace(i1))
        {
            <div class="alert alert-sb alert-sb-info alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-info-circle"></i> @i1
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["InfoMessage"] is string i2 && !string.IsNullOrWhiteSpace(i2))
        {
            <div class="alert alert-sb alert-sb-info alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-info-circle"></i> @i2
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        @* ERROR *@
        @if (TempData["ToastError"] is string e1 && !string.IsNullOrWhiteSpace(e1))
        {
            <div class="alert alert-sb alert-sb-error alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-x-circle"></i> @e1
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["ErrorMessage"] is string e2 && !string.IsNullOrWhiteSpace(e2))
        {
            <div class="alert alert-sb alert-sb-error alert-dismissible fade show px-4" role="alert">
                <i class="bi bi-x-circle"></i> @e2
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
    </div>

    <!-- Page Content -->
    <div class="container-fluid">
        <div class="row g-0">
            @if (User.Identity?.IsAuthenticated == true)
            {
                <!-- Desktop sidebar -->
                <aside class="col-lg-2 sidebar border-end d-none d-lg-block">
                    <nav class="sb-nav d-flex flex-column h-100" aria-label="Primary">
                        <div class="p-3">
                            <a href="/Invoices/Create" class="btn btn-purple w-100 rounded-pill">
                                <i class="bi bi-plus-lg me-1"></i> New invoice
                            </a>
                        </div>

                        <div class="px-3 mt-2 mb-1 text-uppercase small fw-semibold text-white-50">Overview</div>
                        <a class="sb-link d-flex align-items-center @(ViewContext.RouteData.Values["controller"]?.ToString()=="Dashboard" ? "active" : "")"
                           href="/Dashboard" aria-current="@(ViewContext.RouteData.Values["controller"]?.ToString()=="Dashboard" ? "page" : null)">
                            <svg class="sb-ico sb-ico-grid me-2" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                                <rect x="3"  y="3"  width="8" height="8" rx="2"></rect>
                                <rect x="13" y="3"  width="8" height="8" rx="2"></rect>
                                <rect x="3"  y="13" width="8" height="8" rx="2"></rect>
                                <rect x="13" y="13" width="8" height="8" rx="2"></rect>
                            </svg>
                            <span>Dashboard</span>
                        </a>

                        <div class="px-3 mt-3 mb-1 text-uppercase small fw-semibold text-white-50">Work</div>
                        <a class="sb-link d-flex align-items-center @(ViewContext.RouteData.Values["controller"]?.ToString()=="Invoices" ? "active" : "")"
                           href="/Invoices">
                            <i class="bi bi-file-earmark-text me-2"></i>
                            <span>Invoices</span>
                        </a>

                        <a class="sb-link d-flex align-items-center @(ViewContext.RouteData.Values["controller"]?.ToString()=="Clients" ? "active" : "")"
                           href="/Clients">
                            <i class="bi bi-people me-2"></i>
                            <span>Clients</span>
                        </a>

                        <div class="px-3 mt-3 mb-1 text-uppercase small fw-semibold text-white-50">Account</div>
                        <a class="sb-link d-flex align-items-center @(ViewContext.RouteData.Values["controller"]?.ToString()=="ApplicationUser" ? "active" : "")"
                           href="/ApplicationUser/Settings">
                            <i class="bi bi-gear me-2"></i>
                            <span>Settings</span>
                        </a>

                        <div class="mt-auto p-3 small text-white-50">
                            <i class="bi bi-shield-lock me-1"></i> Secure by design
                        </div>
                    </nav>
                </aside>
            }

            <main class="@(User.Identity?.IsAuthenticated == true ? "col-lg-10" : "col-12") pt-2 px-4 pb-4">
                @RenderBody()
            </main>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-light py-4 text-center mt-auto">
        <div class="container">
            <p class="mb-0">&copy; @DateTime.Now.Year SoloBill. All rights reserved.</p>
        </div>
    </footer>

    <!-- Scripts: Bootstrap bundle required for toggler/offcanvas -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Auto-dismiss alerts after 4s
        window.addEventListener('load', () => {
            const alerts = document.querySelectorAll('.alert');
            setTimeout(() => {
                alerts.forEach(a => {
                    const bsAlert = bootstrap.Alert.getOrCreateInstance(a);
                    bsAlert.close();
                });
            }, 4000);
        });
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>